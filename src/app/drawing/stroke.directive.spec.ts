import { StrokeDirective } from './stroke.directive';

describe('StrokeDirective', () => {
  it('should create an instance', () => {
    const directive = new StrokeDirective();
    expect(directive).toBeTruthy();
  });
});
